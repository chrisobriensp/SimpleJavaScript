class SPData {
    url: string;
    constructor(sourceUrl: string) { this.url = sourceUrl; }
    GetData(itemLimit: number = 0) : string {
        var jsonData : string;
        jsonData = 'foobar';
        
        return jsonData;
    } 
    
}